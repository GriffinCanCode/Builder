module infrastructure.config.analysis;

/// Semantic analysis for Builder DSL
/// 
/// This package provides semantic analysis that converts the unified AST
/// into semantic objects (Target, RepositoryRule) that the rest of the
/// system uses.

public import infrastructure.config.analysis.semantic;

