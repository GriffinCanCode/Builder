module runtime.shutdown;

/// Shutdown Coordination Module
/// Explicit resource cleanup and graceful shutdown handling
/// 
/// This module provides a shutdown coordinator for explicit resource cleanup,
/// ensuring caches are properly flushed and resources are released.

public import runtime.shutdown.shutdown;

