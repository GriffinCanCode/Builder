module runtime.watchmode;

/// Watch Mode
/// 
/// Provides continuous file watching and incremental builds.
/// Monitors workspace for changes and automatically triggers rebuilds.

public import runtime.watchmode.watch;
